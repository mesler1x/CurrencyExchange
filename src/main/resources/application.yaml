spring:
  application:
    name: Currency
  datasource:
    url: jdbc:postgresql://${DB_HOST:currency_postgres}:${DB_PORT:5432}/${DB_NAME:currency}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver